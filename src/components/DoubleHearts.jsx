import { motion } from "framer-motion";

// DoubleHearts component intentionally disabled to remove twin hearts from the UI.
const DoubleHearts = () => {
    return null;
};

export default DoubleHearts;